---
import {actions} from 'astro:actions';
import BaseLayout from "../../../layouts/BaseLayout.astro";
import {SchedulesList} from "../../../components/features/users/user-dashboard/schedules/SchedulesList";

const {session, user} = Astro.locals
if (session === null) {
  return Astro.redirect("/");
}
---
<BaseLayout>
    <SchedulesList client:only="solid-js" user={user} />
</BaseLayout>
