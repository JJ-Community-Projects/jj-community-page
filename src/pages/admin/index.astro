---
import {actions} from 'astro:actions';
import BaseLayout from "../../layouts/BaseLayout.astro";
import {AdminPage} from "../../components/features/users/admin/AdminPage.tsx";

const {session, user} = Astro.locals
if (session === null) {
  return Astro.redirect("/");
}

const {data, error} = await Astro.callAction(actions.users.getTiltifyAccount, user.tiltifyName)
if (error) {
  console.error(error)
  return Astro.redirect("/");
}
---
<BaseLayout>
    <AdminPage user={user} client:only="solid-js"/>
</BaseLayout>
