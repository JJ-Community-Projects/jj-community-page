---
import BaseLayout from "../layouts/BaseLayout.astro";
import LoginPage from "../components/features/auth/LoginPage";

const { user, session } = Astro.locals;
if (session) {
  return Astro.redirect(`/admin`);
}
---

<BaseLayout>
  <LoginPage client:only="solid-js" />
</BaseLayout>
